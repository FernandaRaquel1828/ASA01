services:
  app1:
    build: 
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
  app2:
    build: 
      context: ./web01
      dockerfile: Dockerfile
    ports:
      - "81:80"
  

